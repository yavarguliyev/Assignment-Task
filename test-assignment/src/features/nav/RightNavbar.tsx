import { observer } from 'mobx-react-lite';
import React from 'react';
import { Segment } from 'semantic-ui-react';

const RightNavBar: React.FC = () => {
  return (
    <Segment>RightNavBar</Segment>
  )
}

export default observer(RightNavBar);